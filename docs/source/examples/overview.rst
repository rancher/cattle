What to do next?
================

You've got dStack running, what should you do next?  A lot of dStack is currently oriented around Docker.  If you're not too familiar with Docker there are some :ref:`basic examples <docker-example>` using Docker.  Once you've got a handle on Docker, you can then :ref:`deploy some things <dstack-example>` in dStack.
